%h1 Please sign in

#person_search
  = form_for :person, :url => sessions_path do |f|
    =f.text_field :full_name,
      :placeholder => 'What is your name?',
      :autocomplete => 'off',
      :data => {:provide => 'typeahead', :source => Person.full_names },
      :autofocus => true
    =f.submit "It's me!"
  %p.sign_up Couldn't find your name? Click here to #{ link_to "Sign Up",  new_person_path }.
